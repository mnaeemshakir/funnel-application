import types, { NAMESPACE } from './constants';

// context
export const USERCLEAR = `${NAMESPACE}/${types.CLEAR}`;
export const USERSTATE = `${NAMESPACE}/${types.STATE}`;
export const USERVALIDATION = `${NAMESPACE}/${types.VALIDATION}`;
export const CLEAR_USER_VALIDATION_COUPON = `${NAMESPACE}/${types.CLEAR_VALIDATION_COUPON}`;
export const SET_USER_PREMIUMM = `${NAMESPACE}/${types.PREMIUM}`;
export const SET_USER_FRESH_SIGNUP = `${NAMESPACE}/${types.FRESH_SIGNUP}`;
export const SET_USER_LANGUAGE = `${NAMESPACE}/${types.USER_LANGUGE}`;
